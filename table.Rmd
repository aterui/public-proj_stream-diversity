---
title: "Tables"
output:
  pdf_document: default
---

```{r setup, include=FALSE}
  
  knitr::opts_knit$set(root.dir = here::here("empirical"))

```


```{r, echo = FALSE, message=FALSE, results='asis'}

source(normalizePath("../empirical/analysis_lm.R"))

stargazer::stargazer(m_alpha$model, m_beta$model, m_gamma$model,
                     header = FALSE,
                     title = "Estimated parameters of robust linear regression models explaining fish species richness in Hokkaido (Japan) and Midwest (US) regions. Dependent variables were log-10 transformed. Environmental variables (air temperature, precipitation, logit \\% forest) are deviations from the regional averages and were standardized to a mean of zero and a standard deviation of one.",
                     type = "latex",
                     ci = TRUE,
                     covariate.labels = c("$log_{10}$ Watershed area",
                                          "$log_{10}$ Branching probability",
                                          "Region (Midwest vs. Hokkaido)",
                                          "Air temperature",
                                          "Precipitation",
                                          "Logit \\% forest",
                                          "Intercept"),
                     single.row = FALSE,
                     digits = 2,
                     dep.var.labels.include = FALSE,
                     column.labels = c("$\\alpha$ richness", "$\\beta$ richness", "$\\gamma$ richness"),
                     keep.stat = c("rsq"),
                     model.numbers = FALSE)

```

